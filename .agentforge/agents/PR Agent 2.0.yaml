name: PR Agent 2.0
description: PR Agent 2.0 agent
version: 1.0.0
system_prompt: Review all commited changes in this branch, expose in order all the bugs from critical to lower
tools:
  - type: mcp
    name: chrome-devtools
    command: npx
    args:
      - '-y'
      - chrome-devtools-mcp@latest
expose:
  - claude_code
  - codex
  - opencode
  - cursor
skills:
  - ref: AgentForge
  - ref: Test
